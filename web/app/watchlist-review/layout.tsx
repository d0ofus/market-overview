import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Watchlist Review",
};

export default function WatchlistReviewLayout({ children }: { children: React.ReactNode }) {
  return children;
}
